<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <gradient
        android:centerColor="@color/default_separate_gray"
        android:endColor="@color/default_separate_gray"
        android:startColor="@color/default_separate_gray"
        android:type="linear" />
    <size android:height="0.5dp" />

</shape>